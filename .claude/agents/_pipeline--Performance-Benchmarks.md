# Performance Benchmarks

Run Rust + TypeScript benchmarks and verify tests pass

## Pipeline Steps

This agent was auto-generated by AgentFlow from a pipeline definition.

### Step 1: Rust Benchmarks (Shell Command)

cd src-tauri && cargo bench 2>&1

### Step 2: TypeScript Benchmarks (Shell Command)

npx vitest bench --run 2>&1

### Step 3: Verify Tests + Types (Shell Command)

cd src-tauri && cargo test 2>&1 && cd .. && npx tsc --noEmit 2>&1 && echo '--- All 29 Rust tests passed, TypeScript compiles clean ---'

