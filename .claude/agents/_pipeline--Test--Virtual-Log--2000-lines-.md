# Test: Virtual Log (2000 lines)

Generates 2000 log lines to trigger VirtualLogPane (threshold: 500 lines). Compare scrolling performance vs a normal <pre> with 2000 lines.

## Pipeline Steps

This agent was auto-generated by AgentFlow from a pipeline definition.

### Step 1: Flood 2000 Log Lines (Shell Command)

for i in $(seq 1 2000); do echo "[line $i] Processing batch item — status=ok elapsed=${i}ms payload=$(head -c 80 /dev/urandom | base64 | head -c 60)"; done

### Step 2: Small Output (10 lines) (Shell Command)

for i in $(seq 1 10); do echo "Normal log line $i"; done

### Step 3: Done (Shell Command)

echo 'Virtual log test complete. The 2000-line node should use virtualized scrolling, the 10-line node should use plain <pre>.'

